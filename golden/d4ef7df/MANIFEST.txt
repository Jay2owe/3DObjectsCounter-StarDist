reference sha: d4ef7df
runs: 557
fixtures: 22
configurations: 32

Captured from the pre-migration build. Immutable from this point:
harness §7. Detection is NOT exercised — see docs/migration/DETERMINISM.md
for what this set can and cannot certify.

Coverage reductions, stated rather than left implicit:
REDUCED count_254 (~254 objects): runs 1 of 25 map-building configurations. Every measurement configuration still runs.
REDUCED count_255 (~255 objects): runs 1 of 25 map-building configurations. Every measurement configuration still runs.
REDUCED count_256 (~256 objects): runs 1 of 25 map-building configurations. Every measurement configuration still runs.
REDUCED count_65534 (~65534 objects): runs 0 of 25 map-building configurations. Every measurement configuration still runs.
REDUCED count_65535 (~65535 objects): runs 0 of 25 map-building configurations. Every measurement configuration still runs.
REDUCED count_65536 (~65536 objects): runs 0 of 25 map-building configurations. Every measurement configuration still runs.
REDUCED tables and partitions above 512 entries record a digest plus the first and last 32 rows instead of
        every row. Comparison there is exact-only, so a Tier 2 difference inside tolerance reports as a
        failure to diagnose rather than passing quietly.
EXCLUDED ObjectMapBuilder's optional-map memory guard: its outcome depends on available heap and
        therefore cannot be part of a reproducible golden. The reserve is set to zero for the sweep.
